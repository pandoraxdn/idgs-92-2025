import { StyleSheet } from "react-native";

export const appTheme = StyleSheet.create({
    container:{
        justifyContent: "center",
        alignItems: "center",
        alignContent: "center",
        flex: 1
    },
    text:{
        fontSize: 60,
        fontWeight: "bold",
        textAlign: "center",
        color: "violet"
    }
});
